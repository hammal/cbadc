[tox]
envlist = py36, py37, py38
plattform = linux | darwin
isolated_build = True

[testenv]
deps = 
    pytest 
    pytest-cov 
    pytest-benchmark
    sdeint
    deltasigma
commands =
    python -c 'import cbc'
    pytest --ignore=/tests/AnalogToDigital tests